- hosts: web
  become: yes

  tasks:

  - name: update apt
    apt:
      update_cache: yes

  - name: install nginx
    apt:
      name: nginx
      state: present

  - name: start nginx
    service:
      name: nginx
      state: started
      enabled: yes